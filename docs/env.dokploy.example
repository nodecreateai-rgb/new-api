# 复制到 Dokploy → Compose 应用 → Environment 中按需填写（勿提交含真实密码的文件到仓库）。
#
# ========== 必填 ==========
MYSQL_ROOT_PASSWORD=请改为强密码
SQL_DSN=root:请与上一行密码一致@tcp(mysql:3306)/new-api?charset=utf8mb4
SESSION_SECRET=请改为随机长字符串

# ========== 常用可选 ==========
# REDIS_CONN_STRING=redis://:密码@redis:6379/0
# NODE_TYPE=master
# ERROR_LOG_ENABLED=true
# MEMORY_CACHE_ENABLED=true
# BATCH_UPDATE_ENABLED=true
# CRYPTO_SECRET=多机且共用 Redis 时必填
#
# 按次计价的异步任务模型（逗号分隔）。默认已含 sd2-c1,sd2-c2,sd2-c3（视频按条计费）；
# 仅当需要追加其它模型或覆盖列表时才设置；若曾写 seedance2-c* 请改为 sd2-c*。
# TASK_PRICE_PATCH=sd2-c1,sd2-c2,sd2-c3
#
# 主节点且前端与 API 同域时不要设；从节点指向前端绝对 URL 时才需要：
# FRONTEND_BASE_URL=https://你的域名

# ========== 更多变量 ==========
# 完整列表见仓库根目录 .env.example
